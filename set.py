# Python Set

thiset = {"bittu", "aditya", "meshram", "elephent"}
myset = {"apple", "banana", "cat", "dog"}

print("myset: ",myset)   # it print values i.e store in myset 

for i in myset:
    print(i)    # access the set value using loop


# Set is data structure in Python.
# Sets are unordered in Python.
# Sets are mutable in Python.
# Sets not allow to store duplicate value.
# Sets allow to store heterogeneous data.
# Set data is not accessed by their index.
# Sets are dynamic and growable.
# Sets are iterable.
# Sets support nesting (sets inside sets) note: Possible using only frozenset
# Set elements must be hashable (immutable types).
# Sets support set operations (union, intersection, difference).

# note
'''
Set items are unchangeable (immutable)
but
The set itself is changeable (mutable)
'''